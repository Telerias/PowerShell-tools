# Find all accounts that are not disabled and password does not expire
# Requries the ActiveDirectory module to be loaded
# import-module ActiveDirectory
#
search-adaccount –passwordneverexpires | where {$_.enabled}
